<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quartz.zielclient">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.VIBRATE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ziel"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/ZielTheme">
        <service android:name=".fcm.VoiceFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".activities.common.VideoActivity"
            android:configChanges="orientation|screenSize">
        </activity>
        <activity
            android:name=".activities.common.util.SettingsActivity"

            android:label="@string/title_activity_settings"
            android:parentActivityName=".activities.common.VideoActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.quartz.zielclient.activities.common.VideoActivity" />
        </activity>
        <activity
            android:exported="true"
            android:name=".activities.common.SplashScreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.signup.SignUpActivity" />
        <activity android:name=".activities.signup.VerifyPhoneNumberActivity" />
        <activity android:name=".activities.signup.ConfirmationCodeActivity" />
        <activity android:name=".activities.signup.AccountCreationActivity" />
        <activity
            android:name=".activities.assisted.AssistedHomePageActivity"
            android:label="@string/title_activity_home_page" />

        <activity
            android:name=".activities.carer.CarerHomepageActivity"
            android:label="@string/title_activity_carer_homepage"
            android:launchMode="singleTask"/>
        <!--
           The API key for Google Maps-based APIs is defined as a string resource.
           (See the file "res/values/google_maps_api.xml").
           Note that the API key is linked to the encryption key used to sign the APK.
           You need a different API key for each encryption key, including the release key that is used to
           sign the APK for publishing.
           You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_api_key"/>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".activities.common.MapsActivity"
            android:label="@string/title_activity_maps" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".activities.common.TextChatActivity"
            android:label="@string/title_activity_maps" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".activities.carer.CarerMapsActivity"
            android:label="@string/title_activity_carer_maps"
            />

        <activity android:name=".activities.common.StreetViewActivity" />
        <activity android:name=".activities.assisted.AddCarerActivity" />
        <activity android:name=".activities.assisted.AssistedSelectCarerActivity" />
        <activity android:name=".activities.common.VoiceActivity"></activity>
    </application>

</manifest>