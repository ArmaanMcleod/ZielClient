<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quartz.zielclient">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ziel"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/ZielTheme">
        <activity
            android:name=".activities.SplashScreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.signup.SignUpActivity" />
        <activity android:name=".activities.signup.VerifyPhoneNumberActivity" />
        <activity android:name=".activities.signup.ConfirmationCodeActivity" />
        <activity android:name=".activities.signup.AccountCreationActivity" />
        <activity
            android:name=".activities.HomePageActivity"
            android:label="@string/title_activity_home_page" />
        <activity android:name=".activities.common.LaunchPadActivity" />
        <activity
            android:name=".activities.carer.CarerChannel"
            android:label="@string/title_activity_carer_session"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.carer.CarerHomepageActivity"
            android:label="@string/title_activity_carer_homepage" />
        <activity
            android:name=".activities.assisted.AssistedChannel"
            android:label="@string/title_activity_assisted_session"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.common.ManualRedirect"
            android:label="@string/title_activity_manual_redirect"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
           The API key for Google Maps-based APIs is defined as a string resource.
           (See the file "res/values/google_maps_api.xml").
           Note that the API key is linked to the encryption key used to sign the APK.
           You need a different API key for each encryption key, including the release key that is used to
           sign the APK for publishing.
           You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <!--
           The API key for Google Maps-based APIs is defined as a string resource.
           (See the file "res/values/google_maps_api.xml").
           Note that the API key is linked to the encryption key used to sign the APK.
           You need a different API key for each encryption key, including the release key that is used to
           sign the APK for publishing.
           You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".activities.common.MapsActivity"
            android:label="@string/title_activity_maps" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".activities.assisted.AssistedSelectCarer" />
        <activity
            android:name=".activities.common.TextChatActivity"
            android:label="@string/title_activity_maps" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".activities.carer.CarerMapsActivity"
            android:label="@string/title_activity_carer_maps" />
        <activity android:name=".activities.common.StreetViewActivity" />
        <activity android:name=".activities.assisted.AddCarerActivity" />
        <activity android:name=".activities.assisted.AssistedSelectCarerActivity"></activity>
    </application>

</manifest>